# Clearlane — Transaction Data Validator

A browser-based tool that validates transaction CSVs (order, product and
payment data) against configurable rules, then exports a clean dataset
split into chunks. 

**Live demo:**


